import { RedeemClient } from "./components/redeem-client";

export default function RedeemPage() {
  // TODO: Get UUID from searchParams and fetch redemption data
  // const redemptionId = searchParams.id;

  return <RedeemClient />;
}
