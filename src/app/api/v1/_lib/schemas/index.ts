/**
 * Shared API Schema Types
 *
 * Enums and common types used across domain schemas.
 * Domain-specific types are co-located with their routes.
 *
 * Usage:
 *   import { StatusEnum, Inventory } from "../../_lib/schemas";
 */

// Re-export all enums
export * from "./enums";

// Re-export common types
export * from "./common";
